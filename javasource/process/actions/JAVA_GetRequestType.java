// This file was generated by Mendix Modeler.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package process.actions;

import com.mendix.systemwideinterfaces.core.IMendixObject;
import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.webui.CustomJavaAction;

public class JAVA_GetRequestType extends CustomJavaAction<java.lang.Boolean>
{
	private IMendixObject __ProcessRequest;
	private process.proxies.Request ProcessRequest;

	public JAVA_GetRequestType(IContext context, IMendixObject ProcessRequest)
	{
		super(context);
		this.__ProcessRequest = ProcessRequest;
	}

	@Override
	public java.lang.Boolean executeAction() throws Exception
	{
		this.ProcessRequest = __ProcessRequest == null ? null : process.proxies.Request.initialize(getContext(), __ProcessRequest);

		// BEGIN USER CODE
		String fullName = ProcessRequest.getMendixObject().getMetaObject().getName();
		
		String moduleName = ProcessRequest.getMendixObject().getMetaObject().getModuleName();
		
		//remove the module name at the front, plus extra position for the '.' 
		String entityName = fullName.substring(moduleName.length()+1);
		
        ProcessRequest.setRequestType(entityName);
		
		return true;
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 */
	@Override
	public java.lang.String toString()
	{
		return "JAVA_GetRequestType";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
