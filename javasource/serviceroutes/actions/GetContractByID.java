// This file was generated by Mendix Modeler.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package serviceroutes.actions;

import java.util.HashMap;
import java.util.List;
import java.util.Map;
import com.mendix.core.Core;
import com.mendix.systemwideinterfaces.core.IMendixObject;
import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.webui.CustomJavaAction;

public class GetContractByID extends CustomJavaAction<IMendixObject>
{
	private java.lang.String ContractID;

	public GetContractByID(IContext context, java.lang.String ContractID)
	{
		super(context);
		this.ContractID = ContractID;
	}

	@Override
	public IMendixObject executeAction() throws Exception
	{
		// BEGIN USER CODE
		Map<String, String> sort = new HashMap<String, String>();
		sort.put("ContractName", "ASC");
		List<IMendixObject> contracts = Core.retrieveXPathQuery(getContext(), "//" + "SLM.Contract" + "[id" + "='" + ContractID + "']", 1, 0, sort);
		
		return contracts.get(0);
		
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 */
	@Override
	public java.lang.String toString()
	{
		return "GetContractByID";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
