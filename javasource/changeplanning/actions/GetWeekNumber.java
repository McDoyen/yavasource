// This file was generated by Mendix Modeler.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package changeplanning.actions;

import java.util.Calendar;
import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.webui.CustomJavaAction;

public class GetWeekNumber extends CustomJavaAction<java.lang.Long>
{
	private java.util.Date DateForWeekNumber;

	public GetWeekNumber(IContext context, java.util.Date DateForWeekNumber)
	{
		super(context);
		this.DateForWeekNumber = DateForWeekNumber;
	}

	@Override
	public java.lang.Long executeAction() throws Exception
	{
		// BEGIN USER CODE
	    Calendar cal = Calendar.getInstance();
	    cal.setTime(DateForWeekNumber);
	    long wnum = cal.get(Calendar.WEEK_OF_YEAR);		
		
		return wnum;
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 */
	@Override
	public java.lang.String toString()
	{
		return "GetWeekNumber";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
