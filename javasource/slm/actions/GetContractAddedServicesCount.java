// This file was generated by Mendix Modeler.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package slm.actions;

import java.util.Iterator;
import java.util.List;
import slm.proxies.Contract.MemberNames;
import slm.proxies.Service;
import com.mendix.core.Core;
import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.systemwideinterfaces.core.IMendixIdentifier;
import com.mendix.systemwideinterfaces.core.IMendixObject;
import com.mendix.webui.CustomJavaAction;
import communitycommons.XPath;

public class GetContractAddedServicesCount extends CustomJavaAction<java.lang.Long>
{
	private IMendixObject __ContractParameter1;
	private slm.proxies.Contract ContractParameter1;

	public GetContractAddedServicesCount(IContext context, IMendixObject ContractParameter1)
	{
		super(context);
		this.__ContractParameter1 = ContractParameter1;
	}

	@Override
	public java.lang.Long executeAction() throws Exception
	{
		this.ContractParameter1 = __ContractParameter1 == null ? null : slm.proxies.Contract.initialize(getContext(), __ContractParameter1);

		// BEGIN USER CODE
		XPath<Service> serviceXPath = XPath.create(Core.createSystemContext(), Service.class);
		//Include all Service entries which are already related to the contract (from the user context), if none, return empty list
		//The retrieval of the identifiers is done using the getValue method so no database search is invoked (which would be the case if the ContractParameter1.getContract_Service() was used.
		Object valueObject = ContractParameter1.getMendixObject().getValue(this.getContext(), MemberNames.Contract_Service.toString());
		if (valueObject == null){
			return (long) 0;
		}		
		@SuppressWarnings("unchecked")
		List<IMendixIdentifier> addedServiceList = (List<IMendixIdentifier>) valueObject;
		//When there are no related services return an empty list
		if(addedServiceList.size() == 0){
			return (long) 0;
		}
		boolean first = true;
		for(Iterator<IMendixIdentifier> addedServiceIterator = addedServiceList.iterator(); addedServiceIterator.hasNext(); ){
			if(first){
				serviceXPath.subconstraint().eq(XPath.ID,addedServiceIterator.next().toLong());
				first = false;
			}
			else{
				serviceXPath.or().eq(XPath.ID,addedServiceIterator.next().toLong());
			}
		}
		serviceXPath.close();
		//Add search field content
		if(ContractParameter1.getSearchAddedServiceName() != null && !ContractParameter1.getSearchAddedServiceName().equals("")){
			serviceXPath.contains(Service.MemberNames.ServiceName, ContractParameter1.getSearchAddedServiceName());
		}
		if(ContractParameter1.getSearchAddedServiceDescription() != null && !ContractParameter1.getSearchAddedServiceDescription().equals("")){
			serviceXPath.contains(Service.MemberNames.Description, ContractParameter1.getSearchAddedServiceDescription());
		}
		return serviceXPath.count();
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 */
	@Override
	public java.lang.String toString()
	{
		return "GetContractAddedServicesCount";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
