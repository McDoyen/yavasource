// This file was generated by Mendix Modeler.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package slm.actions;

import java.util.Iterator;
import java.util.LinkedList;
import java.util.List;
import nap.proxies.Organisation;
import slm.proxies.Contract.MemberNames;
import com.mendix.core.Core;
import com.mendix.systemwideinterfaces.connectionbus.data.IDataRow;
import com.mendix.systemwideinterfaces.connectionbus.data.IDataTable;
import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.systemwideinterfaces.core.IMendixIdentifier;
import com.mendix.systemwideinterfaces.core.IMendixObject;
import com.mendix.webui.CustomJavaAction;
import communitycommons.XPath;

public class RemoveAllSelectableDeliveringOrganisationsFromContract extends CustomJavaAction<java.lang.Boolean>
{
	private IMendixObject __ContractParameter1;
	private slm.proxies.Contract ContractParameter1;

	public RemoveAllSelectableDeliveringOrganisationsFromContract(IContext context, IMendixObject ContractParameter1)
	{
		super(context);
		this.__ContractParameter1 = ContractParameter1;
	}

	@Override
	public java.lang.Boolean executeAction() throws Exception
	{
		this.ContractParameter1 = __ContractParameter1 == null ? null : slm.proxies.Contract.initialize(getContext(), __ContractParameter1);

		// BEGIN USER CODE
		String oqlQuery = "SELECT " + XPath.ID + 
				 " FROM " + Organisation.getType() +
	             " WHERE ";
		
		//Retrieve all delivering organisation entries which are already related to the contract (from the user context)
		//The retrieval of the identifiers is done using the getValue method so no database search is invoked (which would be the case if the ContractParameter1.getContract_DeliveringOrganisation() was used.
		Object valueObject = ContractParameter1.getMendixObject().getValue(this.getContext(), MemberNames.Contract_DeliveringOrganisation.toString());
		//When there are no related delivering organisations, exit
		boolean first = true;
		if (valueObject != null){
			@SuppressWarnings("unchecked")
			List<IMendixIdentifier> addedDeliveringOrganisationsList = (List<IMendixIdentifier>) valueObject;
			//When there are no related delivering organisations continue
			if(addedDeliveringOrganisationsList.size() != 0){
				String OQLAddedPart = "";
				int OQLAddedCounter = 0;
				for(Iterator<IMendixIdentifier> addedDeliveringOrganisationsIterator = addedDeliveringOrganisationsList.iterator(); addedDeliveringOrganisationsIterator.hasNext(); ){
					String mainIDQueryPart = Organisation.getType() + "/" + XPath.ID + " = " + addedDeliveringOrganisationsIterator.next().toLong();
					if(first){
						OQLAddedPart = OQLAddedPart	+ mainIDQueryPart;
						first = false;
						OQLAddedCounter++;
					}
					OQLAddedPart = OQLAddedPart	+ " OR " + mainIDQueryPart;
					OQLAddedCounter++;
				}
				if(OQLAddedCounter > 1){
					oqlQuery = oqlQuery + "(" + OQLAddedPart + ")";
				}
				else{
					oqlQuery = oqlQuery + OQLAddedPart;
				}
				
				//Add search field content
				String OQLFilterPart = "";
				if(ContractParameter1.getSearchAddedOrganisation() != null && !ContractParameter1.getSearchAddedOrganisation().equals("")){
					String mainSearchAvailableOrganisationQueryPart = Organisation.getType() + "/" + Organisation.MemberNames.OrganisationName + " LIKE '%" + ContractParameter1.getSearchAddedOrganisation() + "%'"; 
					OQLFilterPart = mainSearchAvailableOrganisationQueryPart;
				}
				// 3049 Added check OQLFilterPart.equals("")
				if(!OQLFilterPart.equals(""))	oqlQuery = oqlQuery	+ " AND " +OQLFilterPart;
				
				IDataTable result = Core.retrieveOQLDataTable(this.getContext(), oqlQuery);
				LinkedList<IMendixIdentifier> deliveringOrganisationMendixIdentifierList = new LinkedList<IMendixIdentifier>();
				//Exit on empty result list
				if(result.getRowCount() != 0){
					for(Iterator<IDataRow> iDataRowIterator = result.iterator(); iDataRowIterator.hasNext(); ){
						deliveringOrganisationMendixIdentifierList.add((IMendixIdentifier) iDataRowIterator.next().getValue(this.getContext(), 0));
					}
					LinkedList<IMendixIdentifier> resultList = new LinkedList<IMendixIdentifier>();
					//When the selectable list is < than the added list, remove the selectable entries from the added list, otherwise add the empty list (which empties the relation).
					if(deliveringOrganisationMendixIdentifierList.size() < addedDeliveringOrganisationsList.size()){
						//Add all entries from the added delivering organisations list which are not in the selectable list to the result list
						for(Iterator<IMendixIdentifier> addedDeliveringOrganisationsIterator = addedDeliveringOrganisationsList.iterator(); addedDeliveringOrganisationsIterator.hasNext(); ){
							IMendixIdentifier addedDeliveringOrganisationsIMendixIdentifier = addedDeliveringOrganisationsIterator.next();
							if(!deliveringOrganisationMendixIdentifierList.contains(addedDeliveringOrganisationsIMendixIdentifier)){
								resultList.add(addedDeliveringOrganisationsIMendixIdentifier);
							}
						}
					}
					ContractParameter1.getMendixObject().setValue(getContext(), MemberNames.Contract_DeliveringOrganisation.toString(), resultList);
				}
			}
		}
		return true;
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 */
	@Override
	public java.lang.String toString()
	{
		return "RemoveAllSelectableDeliveringOrganisationsFromContract";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
