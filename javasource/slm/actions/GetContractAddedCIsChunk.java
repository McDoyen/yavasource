// This file was generated by Mendix Modeler.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package slm.actions;

import interfaceconfiguration.proxies.SortOrder;
import java.util.Iterator;
import java.util.LinkedList;
import java.util.List;
import slm.proxies.Contract.MemberNames;
import statetransition.proxies.State;
import cmdb.proxies.CMDBType;
import cmdb.proxies.ConfigurationItem;
import com.mendix.core.Core;
import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.systemwideinterfaces.core.IMendixIdentifier;
import com.mendix.systemwideinterfaces.core.IMendixObject;
import com.mendix.webui.CustomJavaAction;
import communitycommons.XPath;

public class GetContractAddedCIsChunk extends CustomJavaAction<java.util.List<IMendixObject>>
{
	private IMendixObject __ContractParameter1;
	private slm.proxies.Contract ContractParameter1;
	private java.lang.Long Amount;
	private java.lang.Long Offset;

	public GetContractAddedCIsChunk(IContext context, IMendixObject ContractParameter1, java.lang.Long Amount, java.lang.Long Offset)
	{
		super(context);
		this.__ContractParameter1 = ContractParameter1;
		this.Amount = Amount;
		this.Offset = Offset;
	}

	@Override
	public java.util.List<IMendixObject> executeAction() throws Exception
	{
		this.ContractParameter1 = __ContractParameter1 == null ? null : slm.proxies.Contract.initialize(getContext(), __ContractParameter1);

		// BEGIN USER CODE
		XPath<ConfigurationItem> configurationItemXPath = XPath.create(Core.createSystemContext(), ConfigurationItem.class);
		//Include all ConfigurationItem entries which are already related to the contract (from the user context), if none, return empty list
		//The retrieval of the identifiers is done using the getValue method so no database search is invoked (which would be the case if the ContractParameter1.getContract_ConfigurationItem() was used.
		Object valueObject = ContractParameter1.getMendixObject().getValue(this.getContext(), MemberNames.Contract_ConfigurationItem.toString());
		if (valueObject == null){
			return new LinkedList<IMendixObject>();
		}		
		@SuppressWarnings("unchecked")
		List<IMendixIdentifier> addedConfigurationItemList = (List<IMendixIdentifier>) valueObject;
		//When there are no related configuration items return an empty list
		if(addedConfigurationItemList.size() == 0){
			return new LinkedList<IMendixObject>();
		}
		boolean first = true;
		for(Iterator<IMendixIdentifier> addedConfigurationItemIterator = addedConfigurationItemList.iterator(); addedConfigurationItemIterator.hasNext(); ){
			if(first){
				configurationItemXPath.subconstraint().eq(XPath.ID,addedConfigurationItemIterator.next().toLong());
				first = false;
			}
			else{
				configurationItemXPath.or().eq(XPath.ID,addedConfigurationItemIterator.next().toLong());
			}
		}
		configurationItemXPath.close();
		//Add search field content
		if(ContractParameter1.getSearchAddedCIName() != null && !ContractParameter1.getSearchAddedCIName().equals("")){
			configurationItemXPath.contains(ConfigurationItem.MemberNames.Name, ContractParameter1.getSearchAddedCIName());
		}
		if(ContractParameter1.getSearchAddedCITypeName() != null && !ContractParameter1.getSearchAddedCITypeName().equals("")){
			configurationItemXPath.subconstraint(ConfigurationItem.MemberNames.ConfigurationItem_CMDBType, CMDBType.entityName).eq(CMDBType.MemberNames.Name, ContractParameter1.getSearchAddedCITypeName()).addSortingAsc(CMDBType.MemberNames.Name).close();
		}
		if(ContractParameter1.getSearchAddedCIState() != null && !ContractParameter1.getSearchAddedCIState().equals("")){
			configurationItemXPath.subconstraint(ConfigurationItem.MemberNames.ConfigurationItem_States, State.entityName).eq(State.MemberNames.Name, ContractParameter1.getSearchAddedCIState()).close();
		}
		//Set chunk size and offset
		configurationItemXPath.offset(Offset.intValue()).limit(Amount.intValue());
		//Add sorting
		if(ContractParameter1.getSortOrderAdded() != null && ContractParameter1.getSortAttributeCIAdded() != null){
			if(ContractParameter1.getSortOrderAdded() == SortOrder.Ascending){
				switch(ContractParameter1.getSortAttributeCIAdded()){
					case Name:
						configurationItemXPath.addSortingAsc(ConfigurationItem.MemberNames.Name);
						break;
					case Status:
						configurationItemXPath.addSortingAsc(ConfigurationItem.MemberNames.ConfigurationItem_States, State.entityName, State.MemberNames.Name);
						break;
					case _Type:
						configurationItemXPath.addSortingAsc(ConfigurationItem.MemberNames.ConfigurationItem_CMDBType, CMDBType.entityName, CMDBType.MemberNames.Name);
						break;
					default:
						configurationItemXPath.addSortingAsc(ConfigurationItem.MemberNames.Name);
						break;
				}
			}
			else{
				switch(ContractParameter1.getSortAttributeCIAdded()){
					case Name:
						configurationItemXPath.addSortingDesc(ConfigurationItem.MemberNames.Name);
						break;
					case Status:
						configurationItemXPath.addSortingDesc(ConfigurationItem.MemberNames.ConfigurationItem_States, State.entityName, State.MemberNames.Name);
						break;
					case _Type:
						configurationItemXPath.addSortingDesc(ConfigurationItem.MemberNames.ConfigurationItem_CMDBType, CMDBType.entityName, CMDBType.MemberNames.Name);
						break;
					default:
						configurationItemXPath.addSortingDesc(ConfigurationItem.MemberNames.Name);
						break;
				}
			}
		}
		else{
			configurationItemXPath.addSortingAsc(ConfigurationItem.MemberNames.Name);
		}
		List<ConfigurationItem> list = configurationItemXPath.all();
		List<IMendixObject> returnList = new LinkedList<IMendixObject>();
		for(Iterator<ConfigurationItem> listIterator = list.iterator(); listIterator.hasNext(); ){
			returnList.add(listIterator.next().getMendixObject());
		}
		return returnList;
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 */
	@Override
	public java.lang.String toString()
	{
		return "GetContractAddedCIsChunk";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
