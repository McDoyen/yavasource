// This file was generated by Mendix Modeler.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package slm.actions;

import java.util.Iterator;
import nap.proxies.Organisation;
import com.mendix.core.Core;
import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.webui.CustomJavaAction;
import com.mendix.systemwideinterfaces.core.IMendixObject;
import communitycommons.XPath;

public class GetContractAvailableDeliveringOrganisationsCount extends CustomJavaAction<java.lang.Long>
{
	private IMendixObject __ContractParameter1;
	private slm.proxies.Contract ContractParameter1;

	public GetContractAvailableDeliveringOrganisationsCount(IContext context, IMendixObject ContractParameter1)
	{
		super(context);
		this.__ContractParameter1 = ContractParameter1;
	}

	@Override
	public java.lang.Long executeAction() throws Exception
	{
		this.ContractParameter1 = __ContractParameter1 == null ? null : slm.proxies.Contract.initialize(getContext(), __ContractParameter1);

		// BEGIN USER CODE
		XPath<Organisation> deliveringOrganisationXPath = XPath.create(Core.createSystemContext(), Organisation.class);
		//Exclude all ConfigurationItem entries which are already related to the contract (from the user context)
		for(Iterator<Organisation> deliveringOrganisationIterator = ContractParameter1.getContract_DeliveringOrganisation().iterator(); deliveringOrganisationIterator.hasNext(); ){
			Organisation deliveringOrganisation = deliveringOrganisationIterator.next();
			deliveringOrganisationXPath.notEq(XPath.ID,deliveringOrganisation.getMendixObject().getId().toLong());
		}
		//Add search field content
		if(ContractParameter1.getSearchAvailableOrganisation() != null && !ContractParameter1.getSearchAvailableOrganisation().equals("")){
			deliveringOrganisationXPath.contains(Organisation.MemberNames.OrganisationName, ContractParameter1.getSearchAvailableOrganisation());
		}
		return deliveringOrganisationXPath.count();
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 */
	@Override
	public java.lang.String toString()
	{
		return "GetContractAvailableDeliveringOrganisationsCount";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
