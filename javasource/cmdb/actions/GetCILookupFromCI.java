// This file was generated by Mendix Business Modeler 4.0.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package cmdb.actions;

import java.util.List;
import com.mendix.core.Core;
import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.systemwideinterfaces.core.UserAction;
import com.mendix.systemwideinterfaces.core.IMendixObject;

/**
 * 
 */
public class GetCILookupFromCI extends UserAction<IMendixObject>
{
	private IMendixObject __ConfigurationItemPar;
	private cmdb.proxies.ConfigurationItem ConfigurationItemPar;

	public GetCILookupFromCI(IContext context, IMendixObject ConfigurationItemPar)
	{
		super(context);
		this.__ConfigurationItemPar = ConfigurationItemPar;
	}

	@Override
	public IMendixObject executeAction() throws Exception
	{
		this.ConfigurationItemPar = __ConfigurationItemPar == null ? null : cmdb.proxies.ConfigurationItem.initialize(getContext(), __ConfigurationItemPar);

		// BEGIN USER CODE
		String fullName = ConfigurationItemPar.getMendixObject().getMetaObject().getName();
		
		String moduleName = ConfigurationItemPar.getMendixObject().getMetaObject().getModuleName();
		
		//remove the module name at the front, plus extra position for the '.' 
		String entityName = fullName.substring(moduleName.length()+1);
		
		List<IMendixObject> results = Core.retrieveXPathQuery(this.getContext(), "//CMDB.CILookUp[CITypeString='"+entityName+"']");
		if (results!=null && results.size()>0) {
			if (results.size()>1) {
				Core.getLogger("GetCILookupFromCI").error("CI "+entityName+" with multiple CILookups");
			}
			return results.get(0);
		} else {
			Core.getLogger("GetCILookupFromCI").error("CI "+entityName+" with unknown CILookup");
			return null;
		}
        
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 */
	@Override
	public String toString()
	{
		return "GetCILookupFromCI";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
