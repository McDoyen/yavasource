// This file was generated by Mendix Modeler.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package advancedsearch.actions;

import com.mendix.core.Core;
import com.mendix.core.CoreException;
import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.webui.CustomJavaAction;
import advancedsearch.proxies.Index;
import advancedsearch.proxies.SearchField;
import communitycommons.XPath;
import nl.mansystems.advancedsearch.common.IndexConfiguration;

public class updateSearchFields extends CustomJavaAction<java.lang.Boolean>
{
	public updateSearchFields(IContext context)
	{
		super(context);
	}

	@Override
	public java.lang.Boolean executeAction() throws Exception
	{
		// BEGIN USER CODE
		
		for (Index index : Index.values()) {
			String[] fields;

			switch (index) {
				case CMDB:
					fields = IndexConfiguration.CMDBFields;
					break;
				case _Interface:
					fields = IndexConfiguration.InterfaceMessageFields;
					break;
				case Person:
					fields = IndexConfiguration.PersonFields;
					break;
				case Product:
					fields = IndexConfiguration.WebshopFields;
					break;
				case Request:
					fields = IndexConfiguration.RequestFields;
					break;
				default: throw new CoreException("Fields could not be determined");
			}		
			
			for (int i=0; i<fields.length; i++) {
				XPath<SearchField> xpath = XPath.create(context, SearchField.class)
						.eq(SearchField.MemberNames.Index, index)
						.and()
						.eq(SearchField.MemberNames.Field, fields[i]);
				
				if (xpath.count() > 0) {
					continue;
				} else {
					SearchField searchField = new SearchField(context);
					searchField.setIndex(index);
					searchField.setField(fields[i]);
					searchField.commit();
				}
			}
		}
		
		return true;
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 */
	@Override
	public java.lang.String toString()
	{
		return "updateSearchFields";
	}

	// BEGIN EXTRA CODE
	private IContext context = Core.createSystemContext();
	// END EXTRA CODE
}
