// This file was generated by Mendix Modeler.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package replacement.actions;

import java.util.List;
import com.mendix.core.Core;
import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.systemwideinterfaces.core.IMendixObject;
import com.mendix.webui.CustomJavaAction;

public class ReplaceAllCodes extends CustomJavaAction<java.lang.String>
{
	private IMendixObject DataObject;
	private java.lang.String SourceString;
	private java.lang.String LanguageCode;

	public ReplaceAllCodes(IContext context, IMendixObject DataObject, java.lang.String SourceString, java.lang.String LanguageCode)
	{
		super(context);
		this.DataObject = DataObject;
		this.SourceString = SourceString;
		this.LanguageCode = LanguageCode;
	}

	@Override
	public java.lang.String executeAction() throws Exception
	{
		// BEGIN USER CODE
		IContext context = this.getContext();
		
		if (DataObject==null) {
			Core.getLogger("Replacement").debug("Not replacing replacements because null object");
			return SourceString;
		}

		String startSep = (String) Core.getConfiguration().getConstantValue("Replacement.StartSeparator");
		String stopSep = (String) Core.getConfiguration().getConstantValue("Replacement.StopSeparator");
		if (startSep==null) startSep="##";
		if (stopSep==null) stopSep="##";

		List<IMendixObject> iReplacementList = nl.mansystems.replacement.Replacement.getReplacementsFromString(context, SourceString, startSep, stopSep, DataObject);
		String replacedString = nl.mansystems.replacement.Replacement.replaceReplacements(context, SourceString, startSep, stopSep, iReplacementList, DataObject, LanguageCode);
		String cleanedReplacedString = nl.mansystems.replacement.Replacement.cleanRemainingCodes(replacedString, startSep, stopSep);

		return cleanedReplacedString;

		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 */
	@Override
	public java.lang.String toString()
	{
		return "ReplaceAllCodes";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
