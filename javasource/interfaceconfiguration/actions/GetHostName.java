// This file was generated by Mendix Modeler.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package interfaceconfiguration.actions;

import java.net.InetAddress;
import java.net.UnknownHostException;
import communitycommons.Logging;
import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.webui.CustomJavaAction;

public class GetHostName extends CustomJavaAction<java.lang.String>
{
	public GetHostName(IContext context)
	{
		super(context);
	}

	@Override
	public java.lang.String executeAction() throws Exception
	{
		// BEGIN USER CODE
		String computerName = "Could not resolve computername, check log for details.";
		try{
			computerName = InetAddress.getLocalHost().getHostName();
		}
		catch(UnknownHostException uhe){
			Logging.log("Interface_Configuration", communitycommons.proxies.LogLevel.Error, "An error occured during host name resolution, this functionality is used in the interface to determine current host, which in turn is used to determine which interface end points to use.");
		}
		return computerName;
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 */
	@Override
	public java.lang.String toString()
	{
		return "GetHostName";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
