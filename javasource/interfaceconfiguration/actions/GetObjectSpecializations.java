// This file was generated by Mendix Modeler.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package interfaceconfiguration.actions;

import java.util.ArrayList;
import java.util.ListIterator;
import com.mendix.systemwideinterfaces.core.IMendixObject;
import com.mendix.systemwideinterfaces.core.meta.IMetaObject;
import interfaceshared.proxies.StringWrapper;
import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.webui.CustomJavaAction;

public class GetObjectSpecializations extends CustomJavaAction<java.util.List<IMendixObject>>
{
	private IMendixObject AnyObject;

	public GetObjectSpecializations(IContext context, IMendixObject AnyObject)
	{
		super(context);
		this.AnyObject = AnyObject;
	}

	@Override
	public java.util.List<IMendixObject> executeAction() throws Exception
	{
		// BEGIN USER CODE
		ArrayList<IMendixObject> stringWrapperList = new ArrayList<IMendixObject>();    
		ListIterator<? extends IMetaObject> listIterator = AnyObject.getMetaObject().getSubObjects().listIterator();
		while(listIterator.hasNext()){
			StringWrapper stringWrapper = new StringWrapper(this.getContext()); 
			stringWrapper.setValue(listIterator.next().getName());
			stringWrapperList.add(stringWrapper.getMendixObject());
		}
		return stringWrapperList;
		
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 */
	@Override
	public java.lang.String toString()
	{
		return "GetObjectSpecializations";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
