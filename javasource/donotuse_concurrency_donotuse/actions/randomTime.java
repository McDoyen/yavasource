// This file was generated by Mendix Modeler.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package donotuse_concurrency_donotuse.actions;

import org.apache.commons.math3.random.JDKRandomGenerator;
import org.apache.commons.math3.random.RandomGenerator;
import com.mendix.core.Core;
import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.webui.CustomJavaAction;

/**
 * Return a random number between start and stop. Uniform distributed.
 */
public class randomTime extends CustomJavaAction<java.lang.Long>
{
	private java.lang.Long start;
	private java.lang.Long stop;

	public randomTime(IContext context, java.lang.Long start, java.lang.Long stop)
	{
		super(context);
		this.start = start;
		this.stop = stop;
	}

	@Override
	public java.lang.Long executeAction() throws Exception
	{
		// BEGIN USER CODE
		try {
			return start + (long) ((stop - start)*randomGenerator.nextFloat());
		} catch (Exception e) {
			Core.getLogger("ExpertDesk").error("randomTime failed",e);
		}
		return 0L;
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 */
	@Override
	public java.lang.String toString()
	{
		return "randomTime";
	}

	// BEGIN EXTRA CODE
	private static RandomGenerator randomGenerator = new JDKRandomGenerator();
	// END EXTRA CODE
}
