// This file was generated by Mendix Business Modeler 2.5.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package requestanalysis.actions;

import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.systemwideinterfaces.core.UserAction;

/**
 * Get the caption of a Enumeration
 */
public class GetCaptionScaleForQuality extends UserAction<String>
{
	private requestanalysis.proxies.ScaleForQuality Enumeration;

	public GetCaptionScaleForQuality(IContext context, String Enumeration)
	{
		super(context);
		this.Enumeration = Enumeration == null ? null : requestanalysis.proxies.ScaleForQuality.valueOf(Enumeration);
	}

	@Override
	public String executeAction() throws Exception
	{
		// BEGIN USER CODE
		
		String languageCode = getContext().getSession().getLanguage().getCode();
		
		return Enumeration.getCaption(languageCode);
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 */
	@Override
	public String toString()
	{
		return "GetCaptionScaleForQuality";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
