// This file was generated by Mendix Modeler.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package apmagent.actions;

import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.webui.CustomJavaAction;
import com.mendix.webui.FeedbackHelper;

/**
 * xPathQuery starting with //EntityName and optionally with a qualifier between [ and ]
 * sortOrder attribute[:ASC|DESC][;attribute[:ASC|DESC]
 * numberToPreserve when wanting a log table to contain max 100.000 records pass 100.000 to this variable and sort so the last 100.000 records are preserved.
 * 
 */
public class BatchDeleteEntity extends CustomJavaAction<java.lang.Boolean>
{
	private java.lang.String xPathQuery;
	private java.lang.String sortOrder;
	private java.lang.Long numberToPreserve;

	public BatchDeleteEntity(IContext context, java.lang.String xPathQuery, java.lang.String sortOrder, java.lang.Long numberToPreserve)
	{
		super(context);
		this.xPathQuery = xPathQuery;
		this.sortOrder = sortOrder;
		this.numberToPreserve = numberToPreserve;
	}

	@Override
	public java.lang.Boolean executeAction() throws Exception
	{
		// BEGIN USER CODE
        nl.mansystems.shared.SharedFunctions.batchDelete(this.getContext(), xPathQuery, sortOrder, numberToPreserve);
		String entity = xPathQuery.substring(2, (xPathQuery.indexOf('[')>0?xPathQuery.indexOf('['):xPathQuery.length() ) );
		FeedbackHelper.addRefreshClass(getContext(), entity);
        return true;
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 */
	@Override
	public java.lang.String toString()
	{
		return "BatchDeleteEntity";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
