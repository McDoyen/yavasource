// This file was generated by Mendix Modeler.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package emailplus.actions;

import javax.mail.Message;
import com.mendix.systemwideinterfaces.core.IMendixObject;
import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.webui.CustomJavaAction;

public class ReProcessEmailMessageJava extends CustomJavaAction<java.lang.Boolean>
{
	private IMendixObject __EmailMessagePar;
	private emailplus.proxies.EmailMessage EmailMessagePar;

	public ReProcessEmailMessageJava(IContext context, IMendixObject EmailMessagePar)
	{
		super(context);
		this.__EmailMessagePar = EmailMessagePar;
	}

	@Override
	public java.lang.Boolean executeAction() throws Exception
	{
		this.EmailMessagePar = __EmailMessagePar == null ? null : emailplus.proxies.EmailMessage.initialize(getContext(), __EmailMessagePar);

		// BEGIN USER CODE
		String uid = EmailMessagePar.getUID();
		nl.mansystems.email.FetchPop3_EmailPlus fp3 = new nl.mansystems.email.FetchPop3_EmailPlus(this.getContext(), null, false);
		Message message = fp3.getMessage(EmailMessagePar);
		if(message != null) // check if a message was returned
			fp3.processMessage(uid, message, true, true); // added parameters to match new arguments.
		return true;
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 */
	@Override
	public java.lang.String toString()
	{
		return "ReProcessEmailMessageJava";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
