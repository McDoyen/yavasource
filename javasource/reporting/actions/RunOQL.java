// This file was generated by Mendix Modeler.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package reporting.actions;

import com.mendix.core.Core;
import com.mendix.systemwideinterfaces.connectionbus.data.IDataRow;
import com.mendix.systemwideinterfaces.connectionbus.data.IDataTable;
import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.webui.CustomJavaAction;

public class RunOQL extends CustomJavaAction<java.lang.String>
{
	private java.lang.String OQLString;

	public RunOQL(IContext context, java.lang.String OQLString)
	{
		super(context);
		this.OQLString = OQLString;
	}

	@Override
	public java.lang.String executeAction() throws Exception
	{
		// BEGIN USER CODE
		IContext context = this.getContext();
		Core.getLogger("DynamicOQL").debug("Retrieving data");
		Core.getLogger("DynamicOQL").trace("OQL:" + OQLString);
		
		
		IDataTable iDataTable = Core.retrieveOQLDataTable(context, OQLString);
		
		int j=0;
		for (IDataRow dataRow:iDataTable.getRows()) {
			j++;
			StringBuilder rowResultFull = new StringBuilder();
			StringBuilder rowResultShort = new StringBuilder();
			rowResultFull.append("Row " + j + ".\n");
			rowResultShort.append("Row " + j + ": ");
			for (int i=0;i<iDataTable.getSchema().getColumnCount();i++) {
				String columnName = iDataTable.getSchema().getColumnSchema(i).getName();
				String columnType = (iDataTable.getSchema().getColumnSchema(i).getMetaPrimitive()==null?"NULL-TYPE":iDataTable.getSchema().getColumnSchema(i).getMetaPrimitive().getName());
				Object object = dataRow.getValue(context, i);
				rowResultFull.append("Column ").append(columnName).append(" of type ").append(columnType).append(" has value '").append(object==null?"NULL":object.toString()).append("'.\n");
				rowResultShort.append(object==null?"NULL":object.toString()).append("; ");
			}
			Core.getLogger("DynamicOQL").debug(rowResultShort.toString());
			Core.getLogger("DynamicOQL").trace(rowResultFull.toString());
		}
		
		String result = "Results:" + iDataTable.getRowCount();
		return result;
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 */
	@Override
	public java.lang.String toString()
	{
		return "RunOQL";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
