// This file was generated by Mendix Modeler.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package mendix.actions;

import java.util.Calendar;
import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.webui.CustomJavaAction;

public class GetStartOfWeek extends CustomJavaAction<java.util.Date>
{
	private java.util.Date DateInWeek;

	public GetStartOfWeek(IContext context, java.util.Date DateInWeek)
	{
		super(context);
		this.DateInWeek = DateInWeek;
	}

	@Override
	public java.util.Date executeAction() throws Exception
	{
		// BEGIN USER CODE
	    Calendar cal = Calendar.getInstance();
	    cal.setTime(DateInWeek);
	    int dow = cal.get(Calendar.DAY_OF_WEEK);
	    switch (dow) {
		    case Calendar.MONDAY:
		    	break;
		    case Calendar.TUESDAY:
		    	cal.add(Calendar.DATE, -1);
		    	break;
		    case Calendar.WEDNESDAY:
		    	cal.add(Calendar.DATE, -2);
		    	break;
		    case Calendar.THURSDAY:
		    	cal.add(Calendar.DATE, -3);
		    	break;
		    case Calendar.FRIDAY:
		    	cal.add(Calendar.DATE, -4);
		    	break;
		    case Calendar.SATURDAY:
		    	cal.add(Calendar.DATE, -5);
		    	break;
		    case Calendar.SUNDAY:
		    	cal.add(Calendar.DATE, -6);
		    	break;
	    }
	    return cal.getTime();
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 */
	@Override
	public java.lang.String toString()
	{
		return "GetStartOfWeek";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
