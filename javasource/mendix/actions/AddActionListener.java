// This file was generated by Mendix Modeler.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package mendix.actions;

import mendix.licensepoolmanager.LPMLoginListener;
import com.mendix.core.Core;
import com.mendix.core.action.user.LoginAction;
import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.webui.CustomJavaAction;

/**
 * This starts the action listener which checks before logging a user in if the maximum number of concurrent users for a license pool has not yet been exceeded.
 */
public class AddActionListener extends CustomJavaAction<java.lang.Boolean>
{
	public AddActionListener(IContext context)
	{
		super(context);
	}

	@Override
	public java.lang.Boolean executeAction() throws Exception
	{
		// BEGIN USER CODE
		Core.addListener(new LPMLoginListener(LoginAction.class));
		//Core.addUserAction(LPMLoginAction.class.getName()); 
		Core.getLogger("LicensePoolManager").info("LicensePoolManager login listener started.");
		
		return true;
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 */
	@Override
	public java.lang.String toString()
	{
		return "AddActionListener";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
