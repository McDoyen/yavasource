// This file was generated by Mendix Modeler.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package mendix.actions;

import com.mendix.core.Core;
import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.systemwideinterfaces.core.ISession;
import com.mendix.systemwideinterfaces.core.IUser;
import com.mendix.webui.CustomJavaAction;
import com.mendix.systemwideinterfaces.core.IMendixObject;

public class CheckCurrentPassword extends CustomJavaAction<java.lang.Boolean>
{
	private IMendixObject __UserParameter1;
	private system.proxies.User UserParameter1;
	private java.lang.String Password;

	public CheckCurrentPassword(IContext context, IMendixObject UserParameter1, java.lang.String Password)
	{
		super(context);
		this.__UserParameter1 = UserParameter1;
		this.Password = Password;
	}

	@Override
	public java.lang.Boolean executeAction() throws Exception
	{
		this.UserParameter1 = __UserParameter1 == null ? null : system.proxies.User.initialize(getContext(), __UserParameter1);

		// BEGIN USER CODE
		
		IContext context = this.getContext();
		ISession session = context.getSession();
		IUser user = session.getUser();
		
		if (this.UserParameter1 != null && !this.Password.equalsIgnoreCase("")) {
			return Core.authenticate(this.getContext(), user, this.Password);
		} else {
			return false;
		}
		
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 */
	@Override
	public java.lang.String toString()
	{
		return "CheckCurrentPassword";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
