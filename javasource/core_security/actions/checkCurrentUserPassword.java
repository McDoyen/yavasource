// This file was generated by Mendix Modeler.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package core_security.actions;

import com.mendix.core.Core;
import com.mendix.core.CoreException;
import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.systemwideinterfaces.core.IMendixObject;
import com.mendix.systemwideinterfaces.core.IUser;
import com.mendix.webui.CustomJavaAction;

public class checkCurrentUserPassword extends CustomJavaAction<java.lang.Boolean>
{
	private java.lang.String password;

	public checkCurrentUserPassword(IContext context, java.lang.String password)
	{
		super(context);
		this.password = password;
	}

	@Override
	public java.lang.Boolean executeAction() throws Exception
	{
		// BEGIN USER CODE
		IUser user = getContext().getSession().getUser();
		if (user != null) {
			return Core.authenticate(getContext(), user, password);
		} else {
			throw new CoreException("Cannot determine current user");
		}
		
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 */
	@Override
	public java.lang.String toString()
	{
		return "checkCurrentUserPassword";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
