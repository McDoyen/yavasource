// This file was generated by Mendix Modeler.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package queryrequests.actions;

import java.util.HashMap;
import java.util.List;
import java.util.Map;
import queryrequests.proxies.SearchResults;
import com.mendix.core.Core;
import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.systemwideinterfaces.core.IMendixIdentifier;
import com.mendix.systemwideinterfaces.core.IMendixObject;
import com.mendix.webui.CustomJavaAction;

public class GetRequestByXpath extends CustomJavaAction<IMendixObject>
{
	private java.lang.String XpathQuery;
	private java.lang.Long MaxResults;

	public GetRequestByXpath(IContext context, java.lang.String XpathQuery, java.lang.Long MaxResults)
	{
		super(context);
		this.XpathQuery = XpathQuery;
		this.MaxResults = MaxResults;
	}

	@Override
	public IMendixObject executeAction() throws Exception
	{
		// BEGIN USER CODE
		
		Map<String, String> sortMap = new HashMap<String,String>();
		sortMap.put("TicketID", "DSC");
		IContext context = this.getContext();
		
		SearchResults searchResults = new SearchResults(this.getContext());

		List<IMendixObject> requests = Core.retrieveXPathQuery(context, XpathQuery,MaxResults.intValue(),0,sortMap);		
		java.util.List<IMendixIdentifier> identifiers = new java.util.ArrayList<IMendixIdentifier>();
					
		for (IMendixObject request : requests) {			
			IMendixIdentifier id = (IMendixIdentifier)request.getMember(context, process.proxies.Request.MemberNames.OverviewRequest_Request.toString()).getOriginalValue(context);
			if(id != null){
				identifiers.add(id);
			}
		}
		
		if (identifiers != null) {
			searchResults.getMendixObject().setValue(context, queryrequests.proxies.SearchResults.MemberNames.SearchResults_OverviewRequest.toString(), identifiers);
		}		
		Core.commitWithoutEvents(context, searchResults.getMendixObject());
		
		return searchResults.getMendixObject();
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 */
	@Override
	public java.lang.String toString()
	{
		return "GetRequestByXpath";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
